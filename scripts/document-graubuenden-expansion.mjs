import { writeFile } from 'node:fs/promises'
import { readJson } from './build-graubuenden-region.mjs'
const review=await readJson('data/graubuenden-audit/access-road-review.json'), seasonal=await readJson('data/graubuenden-audit/seasonal-summary.json'), validation=await readJson('data/graubuenden-audit/seasonal-validation.json')
const f=n=>n.toLocaleString('en-CH'), clean=s=>String(s).replaceAll('|','/').replaceAll('\n',' ')
const bus=['# Graubünden: complete bus-pattern recovery','',
'This review adds **145 Friday and 119 Sunday complete journeys** to the initial study. Bus admission is now **5,691 / 5,762** and **4,484 / 4,561**. After the subsequent [Bern/Basel rail review](GRAUBUENDEN-RAIL-COMPLETION.md), rail admits 854 / 856 and 840 / 842; the application has **6,545** and **5,324** complete journeys. The whole-canton denominators remain 39,402 and 38,425, including unsupported mountain, boat and tram candidates. These are numerical geometry checks, not operator certification.','',
'[Machine-readable before/after review](../data/graubuenden-audit/access-road-review.json) · [Explicit admission policy](../data/graubuenden-access-roads/policy.json) · [Source pins](../data/graubuenden-access-roads/sources.json) · [Every annual route](GRAUBUENDEN-ROUTE-INVENTORY.md)','',
'## Scope and admission','',
'Every one of the **24 route records with a previously excluded bus journey** was investigated, using all **187 complete fixture patterns** on those routes. The pfaedle run produces 155 entirely matched trial patterns. **35** newly recovering road-pattern identities are explicitly admitted. Existing complete primary patterns keep their exact paths and provenance, even if an alternative run also succeeds. All 5,546 previously admitted Friday buses and 4,365 Sunday buses are identical. A successful subset of a failed pattern is never spliced into another run; every newly admitted journey uses one complete pattern from the supplemental run. Full GTFS calls, times, sequence, call rules and external termini remain unchanged.','',
'The route-level table below accounts for all 24 trials, including routes with no gain. The machine-readable audit assigns a disposition to every one of the 187 patterns and retains every remaining primary failure and trial failure.','',
'| Route / agency / line | Trial patterns | Friday gained / still excluded | Sunday gained / still excluded |','| --- | ---: | ---: | ---: |']
for(const r of review.routes)bus.push(`| ${r.routeId} / ${r.agencyId} / ${r.line} | ${r.trialPatterns} | ${r.days.map(d=>`${d.gained} / ${d.remainingExcluded}`).join(' | ')} |`)
bus.push('', '## OSM graph and failed reuse experiment','',
'The preserved Geofabrik Switzerland PBF from 2 September 2026 has SHA-256 `39257b1c92a45da38ca94ddb745bdcf53551d0e66b02d89f3de3e7c064c3a29f`. A fresh graph was filtered using the complete Graubünden trial feed and the reviewed service-road configuration. Unlike the primary Swiss-plus-border graph, this supplemental graph has only the geographical coverage available in the Swiss extract. Foreign coverage is not assumed. The filtered graph hash is `06e1be74a400233fa0bfbda8a37e7802b3e5a40b6d6e8a37f941edec275edf5c`. Its '+f(review.currentGraph.nodeCount)+' nodes span '+review.currentGraph.bounds.join(', ')+' (west, south, east, north); '+f(review.currentGraph.graubuendenBoxNodeCount)+' are inside the diagnostic [9, 46.2, 10.5, 47.1] box. That box is an overlap diagnostic, not canton membership.','',
'The first experiment reused the preserved Luzern access graph. Despite its national-source wording, that filtered artifact spans only [7.3402929, 46.6281758, 8.5914958, 47.4548652] and has zero nodes in the Graubünden diagnostic box. All 187 trial patterns failed. The underlying pfaedle filtering uses input-dependent bounding boxes as well as tag filters: a national parent PBF does not make each filtered output national. The failed cache, matcher outputs and original source receipt remain in [rejected-reuse](../data/graubuenden-access-roads/rejected-reuse/sources.json), with an explicit [extent correction](../data/graubuenden-access-roads/rejected-reuse/extent-review.json). Only its pinned configurations and executable identity were reused for the corrected run. No rejected trial enters the application.','',
'The filter retains highway=service; the routing configuration uses 20 m stop-candidate and edge-snap limits. It does not remove OSM access or turn restrictions. The importer retains the primary 120 m endpoint snap, max(1,500 m, 6 × direct distance) detour and 5 m simplification limits. Maximum measured trial snap is 26.76 m. Full matcher warnings, original shapes and shape distances are archived; explicit fallback hops are rejected. The run matches 16,045 / 16,295 segment occurrences, rejecting 43 pattern-segment occurrences. Pair success is not whole-journey admission. Tightening candidate distance and retaining service roads does not prove physical vehicle direction, seasonal access or every legal restriction.','',
'## Official local evidence and reuse','',
'PostAuto’s [Chur–Flims–Laax–Ilanz network diagram](https://www1.postauto.ch/-/media/postauto/fahrplan-und-netz/liniennetz/dokumente/graubuenden/chur-flims-laax-ilanz.pdf?vs=9) and [Chur–Mittelbünden diagram](https://www.postauto.ch/-/media/postauto/fahrplan-und-netz/liniennetz/dokumente/graubuenden/chur-mittelbuenden.pdf?vs=6), both valid from 14 December 2025 and checked on 8 September 2026, support line/stop identity around Laax and Alvaneu. They are schematic network diagrams; no coordinates or road alignments are traced from them. They do not certify every platform-specific fixture pattern or replacement service. GeoGR’s [help page](https://geogr.ch/hilfe) explains access to public geodata but supplies no dataset-specific operational bus-vector permission. The earlier cantonal endpoint and metadata findings remain documented in the [initial study](GRAUBUENDEN-STUDY.md). A verified local operational line export and its own terms are still outstanding.','',
'Bus geometry remains **© OpenStreetMap contributors**, [ODbL 1.0](https://www.openstreetmap.org/copyright). The complete supplemental [derived path database](../public/data/graubuenden-region/access-road-paths.json) includes pattern mapping and attribution, alongside the primary database. Preserve ODbL attribution and share-alike database obligations. The executable has separate GPL licensing, preserved in pfaedle-LICENSE. Timetable and FOT attribution/terms remain as documented in the initial study. Official diagrams are linked as evidence; no right to redistribute or derive routable geometry from those diagrams is claimed.','',
'## Reproduction','',
'Offline validation uses the committed, hashed graph and complete matcher outputs. Fresh acquisition or matching changes receipts and requires an explicit policy review; the build does not update trust pins automatically. Preparation computes rejected routes with the supplemental review disabled so it remains reproducible after the gain.','',
'```sh','node scripts/prepare-graubuenden-access-roads.mjs','node scripts/match-postbus-roads.mjs \\','  --pfaedle /private/tmp/gleislicht-pfaedle/build/pfaedle \\','  --osm /private/tmp/graubuenden-access-network.osm \\','  --config data/graubuenden-access-roads/routing.cfg \\','  --feed /private/tmp/graubuenden-access-feed \\','  --output /private/tmp/graubuenden-access-matched','node scripts/import-graubuenden-access-roads.mjs','# Explicitly review source and policy hashes before rebuilding.','node scripts/build-graubuenden-region.mjs','node scripts/check-graubuenden-region.mjs','node scripts/review-graubuenden-access-roads.mjs','node scripts/review-graubuenden-rail.mjs','```','')
await writeFile('docs/GRAUBUENDEN-BUS-REVIEW.md',bus.join('\n'))
const out=['# Graubünden: twelve-date seasonal timetable inventory','',
'This is an **audit-only** expansion of the same pinned 20260902 national timetable and complete Graubünden canton polygon. It preserves all **380 annual route records**, **67 agency identities** and **79,029 annual source trip records**. The application still offers only **4 and 6 September 2026**. No additional seasonal geometry has been admitted.','',
'[Seasonal summary and all route counts](../data/graubuenden-audit/seasonal-summary.json) · [Complete pattern audit, gzip](../data/graubuenden-audit/seasonal-patterns.json.gz) · [Source validation](../data/graubuenden-audit/seasonal-validation.json) · [Original study and reuse terms](GRAUBUENDEN-STUDY.md)','',
'The sample covers winter Friday/Sunday, Good Friday/Easter Sunday, summer Friday/Sunday, Swiss National Day, the two released September dates, late October Friday/Sunday and the last Friday before the annual timetable change. These are twelve samples, not daily validation of the whole timetable year. **58** of the 87 route records inactive on both September fixtures become active on at least one other sample; **29** remain inactive on all twelve. No claim of permanent closure or absent service follows from inactivity.','',
'## Full candidate counts','',
'“Reviewed match” means exact route, direction, ordered platform and pickup/drop-off-rule identity with an admitted September pattern. It is a candidate for reusing that geometry after seasonal review, not an admission. “Known exclusion” matches a rejected September pattern, including unsupported modes. “New pattern” has no September pattern identity and needs review. All categories retain headway representatives and preceding-service-day carry-ins in their totals.','',
'| Date | Journeys | Patterns | Reviewed match | Known exclusion | New pattern | Headway representatives | Carry-ins |','| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |']
for(const d of seasonal.days)out.push(`| ${d.date} | ${f(d.journeys)} | ${f(d.patternCount)} | ${f(d.byStatus['matches-reviewed-September-pattern'])} | ${f(d.byStatus['known-September-geometry-exclusion'])} | ${f(d.byStatus['unreviewed-seasonal-pattern'])} | ${f(d.headwayJourneys)} | ${f(d.carryIns)} |`)
out.push('', 'There are no prior-arrangement-call instances in these twelve samples; the explicit status and exclusion rule remain in the audit. Headway instances with exact_times=0 describe interval-grid representatives, not exact departures. Large differences between dates include frequency-based mountain services and seasonal calendars; candidate movement counts must not be interpreted as observed vehicle counts.','',
'## Completeness and limits','',
'An independent validation rereads the pinned national ZIP, checks its SHA-256, streams all **'+f(validation.annualStopTimeRows)+' stop-time rows**, and verifies **'+f(validation.days.reduce((n,d)=>n+d.journeys,0))+'** emitted complete journey instances against **'+f(validation.sourceTripRecords)+'** distinct original trip records. It checks calendar additions/removals, original route/direction, every ordered call and sequence, every original time and call rule, carry-in offsets and interval-anchored frequency grids. September snapshots must exactly equal the original source snapshots, and annual route identities and polygon stop sets must remain identical. The validation certifies the emitted records against source evidence; twelve samples cannot establish daily annual coverage.','',
'The cache uses service-day seconds and the established fixed civil-day carry-in offsets. **25 October 2026 is the daylight-saving fall-back date**: the repeated local hour is not disambiguated into elapsed-time instants. That fixture is a timetable-pattern inventory only; neither a 25-hour animation nor observed chronological motion is claimed. All extra dates remain audit-only. Winter road closures, ski shuttle operation, pass access, temporary diversions, current rail alignment and physical direction require separate dated evidence before geometry reuse.','',
'## Routes newly active outside the released fixtures','',
'Every route remains in the machine-readable 380-route inventory, including all zero counts. This table identifies all 58 newly observed active records; maximum counts are from the twelve samples and do not imply daily service.','',
'| Route | Agency | Mode / line | Active samples | Maximum journeys in a sample |','| --- | --- | --- | ---: | ---: |')
for(const id of seasonal.newlyActiveRoutes){const r=seasonal.routes.find(r=>r.routeId===id);out.push(`| ${id} | ${r.agencyId} / ${clean(r.agency)} | ${r.mode} / ${clean(r.line)} | ${r.days.filter(d=>d.journeys).length} | ${f(Math.max(...r.days.map(d=>d.journeys)))} |`)}
out.push('', '## Records inactive on all twelve samples','', '| Route | Agency | Mode / line |','| --- | --- | --- |')
for(const id of seasonal.stillInactiveRoutes){const r=seasonal.routes.find(r=>r.routeId===id);out.push(`| ${id} | ${r.agencyId} / ${clean(r.agency)} | ${r.mode} / ${clean(r.line)} |`)}
out.push('', '## Reproduction and attribution','', 'Timetable: **SBB / opentransportdata.swiss**, under the [platform terms](https://opentransportdata.swiss/en/terms-of-use/); boundary: **© swisstopo**, under its [open geodata terms](https://www.swisstopo.admin.ch/de/nutzungsbedingungen-kostenlose-geodaten-und-geodienste). This is a deliberately dated research archive, not a current journey planner. The pattern comparison references the separately attributed FOT/OSM September study; it adds no new seasonal geometry database.','',
'```sh','node --max-old-space-size=8192 scripts/prepare-graubuenden-seasonal.mjs /private/tmp/GTFS_FP2026_20260902.zip','node --max-old-space-size=8192 scripts/graubuenden-seasonal.mjs','node --max-old-space-size=8192 scripts/check-graubuenden-seasonal.mjs /private/tmp/GTFS_FP2026_20260902.zip','node scripts/document-graubuenden-expansion.mjs','```','')
await writeFile('docs/GRAUBUENDEN-SEASONAL-STUDY.md',out.join('\n'))
console.log('Documented all bus trials and the twelve-date seasonal inventory.')
